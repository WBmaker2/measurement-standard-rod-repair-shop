# 측정 기준봉 정비소 MVP 구현 계획

**목표:** 초등 1~2학년 학생이 화면 속 물체를 재는 가상 기준봉에서 시작점 어긋남, 단위칸 사이의 틈, 단위칸 겹침, 서로 다른 단위 크기, 눈금선과 눈금 사이 개수 혼동을 찾아 고친 뒤 같은 물체를 다시 재어 측정값이 달라진 이유를 설명하는 서버 없는 수학 학습 웹앱을 구현합니다.

**핵심 학습 경험:** 학생은 화면 속 자의 끝 숫자만 읽지 않습니다. 먼저 물체 한 끝과 0의 위치를 맞추고, 같은 크기의 단위칸이 틈과 겹침 없이 반복되는지 살핍니다. `틈 닫기`, `겹침 풀기`, `같은 크기로 맞추기`, `0에 맞추기` 버튼으로 도구를 고친 뒤 눈금선 개수가 아니라 눈금 사이의 같은 단위칸을 세어 다시 측정합니다. 정비 전후의 값이 왜 달랐는지는 빠진 공간·겹친 공간·어긋난 시작점과 연결합니다.

**권장 아키텍처:** 향후 별도 저장소 `measurement-standard-rod-repair-shop`에 Vite, React, TypeScript 기반 정적 앱을 구성합니다. CSS 픽셀이 아닌 정수 모형 좌표로 물체와 단위 구간을 표현하는 `MeasurementModel`, 시작점·틈·겹침·단위 크기·눈금 라벨 오류를 검사하는 `ToolInspector`, 검수된 수리 동작만 적용하는 `RepairReducer`, SVG 기준봉과 같은 내용을 제공하는 텍스트 단위표를 분리합니다. 서버, 로그인, 생성형 AI, 카메라·AR, 실제 화면 보정, 외부 API, 브라우저 영구 저장은 사용하지 않습니다.

**기술 스택:** Vite, React, TypeScript, CSS, SVG, Vitest, React Testing Library, Playwright, `@axe-core/playwright`, Node.js

**현재 상태:** 이 문서는 구현 계획만 정의합니다. 별도 구현 요청 전에는 새 저장소 생성, 패키지 설치, 앱 코드·삽화·미션 데이터 제작, 테스트 실행, 커밋, 푸시, 배포, HVC 등록, 공개 갤러리 동기화를 하지 않습니다.

## 1. 확인 자료와 설계 전제

### 확인 자료

- 아이디어 출처: [`docs/2026-07-17-next-education-webapp-ideas.md`](../../2026-07-17-next-education-webapp-ideas.md)
- 기존 앱 비교: [`docs/vibehong-webapp-catalog.md`](../../vibehong-webapp-catalog.md), 2026-07-17 문서 기준 71개 주제
- 직접 비교 앱: `째깍째깍 시간 탐험대`, `도전! 디지털 수평잡기 실험실`
- 인접 비교 앱: `십 묶음 택배 터미널`, `분류 기준 뒤집개`, `요리조리 도형 변신 마법사`, `AI 각도 챌린지`
- 성취기준 원문: `★(초)2022개정교육과정교과별성취수준(1~2학년군).pdf`
- `[2수03-06]`은 PDF 뷰어 36쪽, 문서 내부 인쇄 32쪽에서 확인했습니다.
- `[2수03-10]`~`[2수03-12]`는 PDF 뷰어 37쪽, 문서 내부 인쇄 33쪽에서 확인했습니다.

### 기존 대화에서 이어받는 원칙

- 기존 앱과 소재만 다르고 핵심 조작·학습 순환이 같은 아이디어를 만들지 않습니다.
- 화면의 단위칸은 실제 기기에서 물리적인 1cm 또는 1m가 아니며 `1cm를 나타내는 모형칸`이라고 설명합니다.
- 학생이 드래그하지 않아도 모든 수리와 측정을 버튼·키보드로 완료할 수 있어야 합니다.
- 시작점·틈·겹침·단위 반복을 한꺼번에 고치게 하지 않고 초기 미션에서는 한 오류씩 다룹니다.
- 눈금 숫자만 맞히는 퀴즈보다 단위가 같은 크기로 빈틈없이 반복되는 이유를 중심에 둡니다.
- 점수·속도·연속 정답보다 발견한 오류, 적용한 수리, 다시 잰 값, 전후 차이의 이유를 기록합니다.
- 실제 구현 시 화면의 적절한 위치에 작은 `업데이트 내역` 버튼을 두고 개발일과 개선일을 최신순으로 기록합니다.
- 모든 코드 파일은 500줄 미만으로 유지하고 기능별로 분리합니다.

## 2. 교육과정 연결

### 직접·부분 연결 표

| 역할 | 성취기준·성취수준 | 앱에서 지원하는 활동 | 연결 한계 | 원문 위치 |
|---|---|---|---|---|
| 기초·부분 연결 | `[2수03-06] 구체물의 길이, 들이, 무게, 넓이를 비교하여 각각 ‘길다, 짧다’, ‘많다, 적다’, ‘무겁다, 가볍다’, ‘넓다, 좁다’ 등을 구별하여 말할 수 있다.` | 두 가상 물체의 한쪽 끝을 맞추어 직접 비교하고, 정비된 같은 단위 도구로 잰 길이를 이용해 더 길다·더 짧다를 말합니다. | 길이만 다루며 실제 구체물, 들이·무게·넓이 비교는 지원하지 않습니다. | 1~2학년군 PDF 36쪽 |
| 핵심·부분 연결 | `[2수03-10] 길이 단위 1cm와 1m를 알고, 이를 이용하여 주변 사물의 길이를 측정할 수 있다.` | `1cm를 나타내는 같은 크기의 모형칸`과 0 시작점을 사용해 가상 물체를 재고, 잘못된 기준봉을 고쳐 표준 단위가 일관되어야 하는 이유를 확인합니다. | 화면 자체가 실제 1cm가 아니며 주변 실물 측정을 수행하지 않으므로 성취기준 전체 달성을 주장하지 않습니다. | 1~2학년군 PDF 37쪽 |
| 제한적 연결 | `[2수03-11] 1m와 1cm의 관계를 이해하고, 길이를 ‘몇 m 몇 cm’와 ‘몇 cm’로 표현할 수 있다.` | 선택형 `큰 기준봉 관계 카드`에서 1m 모형 막대가 1cm 모형칸 100개와 같은 관계임을 10개씩 묶어 확인합니다. | 100개 눈금을 직접 세거나 복합 단위 변환 문제를 핵심 미션으로 다루지 않습니다. | 1~2학년군 PDF 37쪽 |
| 보조·부분 연결 | `[2수03-12] 여러 가지 물건의 길이를 어림하고, 길이에 대한 양감을 기른다.` | 측정 전에 `5cm 모형보다 짧음·비슷함·김` 또는 세 개의 가까운 수 중 하나로 어림하고 정비된 기준봉 결과와 비교합니다. | 화면 크기가 실제 물리 길이가 아니므로 실생활 1cm·1m 양감 형성은 실제 물건 활동이 필요합니다. | 1~2학년군 PDF 37쪽 |

### 성취수준 적용 원칙

- `[2수03-06]` C·B 수준의 길다·짧다 구별을 한쪽 끝 맞추기와 결과 비교로 지원합니다.
- `[2수03-10]` C 수준의 안내된 측정 절차를 안내 미션에서 단계적으로 제공합니다.
- `[2수03-10]` B 수준의 표준 단위 필요성은 서로 다른 크기의 단위칸이 같은 물체에 다른 값을 만드는 사건으로 확인합니다.
- `[2수03-10]` A 수준의 임의 단위·표준 단위 비교는 마지막 미션과 교사용 후속 활동에서 일부 지원합니다.
- `[2수03-11]`의 1m와 1cm 관계는 보조 카드로만 제공하고 앱 핵심 완료 조건이나 학생 등급에 사용하지 않습니다.
- `[2수03-12]` B 수준의 어림과 실제 잰 값 비교를 첫 예측·정비 후 확인으로 일부 지원합니다.
- A·B·C는 학생 화면의 점수·등급·난이도 이름으로 노출하지 않습니다.
- 교사용 관찰 항목은 `한쪽 끝 맞추기`, `같은 단위 반복`, `틈·겹침 발견`, `눈금 사이 세기`, `어림과 측정 비교`, `모형과 실물 구분`으로 제한합니다.

## 3. 측정 개념과 오개념 방지 계약

### 학생용·교사용 용어 구분

| 구분 | 학생 화면 표현 | 교사용 설명 | 사용하지 않을 오해 표현 |
|---|---|---|---|
| 모형 단위칸 | 1cm를 나타내는 모형칸 | 화면 안에서 길이 관계를 나타내는 동일한 추상 단위 | 화면의 이 칸은 실제로 언제나 1cm다 |
| 시작점 | 물체 한 끝과 0을 맞추는 곳 | 초보 측정 절차에서 기준봉의 0 경계와 대상 시작 경계를 정렬 | 어떤 자든 0이 아니면 절대 잴 수 없다 |
| 눈금선 | 단위칸의 경계 | 인접한 단위 구간을 나누는 경계 표시 | 눈금선 하나가 길이 1cm다 |
| 눈금 사이 | 길이 1단위가 놓인 구간 | 두 경계 사이의 동일한 단위 구간 | 0부터 5까지 선이 6개이므로 6cm다 |
| 틈 | 단위칸 사이의 빈 공간 | 측정에 포함되어서는 안 되는 빈 구간 | 빈 공간도 단위 한 칸이다 |
| 겹침 | 두 단위칸이 같은 부분을 함께 덮음 | 대상 일부를 중복해서 세게 만드는 잘못된 배치 | 겹친 칸도 각각 온전한 길이다 |
| 같은 단위 | 모든 단위칸의 크기가 같음 | 측정값 비교를 가능하게 하는 단위 반복의 일관성 | 단위 이름만 같으면 크기가 달라도 된다 |

### 고정 수학 원칙

- 초보 측정에서는 물체 한 끝을 기준봉의 0과 맞추고 다른 끝이 닿는 눈금을 읽습니다.
- 길이는 눈금선의 개수가 아니라 물체가 차지한 눈금 사이의 같은 단위 구간 수입니다.
- 같은 단위칸은 크기가 같고, 틈이나 겹침 없이 이어져야 합니다.
- 단위칸 사이에 틈이 있으면 빈 공간까지 길이에 포함되어 잘못된 값이 나올 수 있습니다.
- 단위칸이 겹치면 같은 부분을 여러 번 세어 잘못된 값이 나올 수 있습니다.
- 단위칸 크기가 다르면 단위 개수만으로 물체 길이를 믿거나 다른 측정과 비교하기 어렵습니다.
- 같은 물체를 같은 표준 단위와 바른 절차로 재면 같은 측정값이 나와야 합니다.
- 자의 0 부분이 손상된 경우 다른 눈금에서 시작해 두 눈금의 차로 잴 수도 있지만, 이는 저학년 MVP 범위에서 제외합니다.
- 1m는 100cm 관계이지만 화면의 1m 모형 막대가 실제 기기에서 1m 길이로 보이는 것은 아닙니다.
- 실제 길이 양감은 화면만으로 완성되지 않으며 실제 자·줄자·물체 측정 활동이 필요합니다.

### 고정 안내 문구

- `화면의 칸은 1cm를 나타내는 모형이에요. 화면에서 실제 1cm 크기는 아니에요.`
- `물체 한 끝과 0을 맞춰요.`
- `같은 크기의 단위칸을 틈과 겹침 없이 이어 놓아요.`
- `눈금선을 세지 말고 눈금 사이의 칸을 세어요.`
- `고장난 도구의 숫자가 달라도 물체 길이 자체가 바뀐 것은 아니에요.`
- `실제 물건은 실제 자나 줄자로 다시 재어 보아요.`

## 4. 제품 접근 비교와 결정

| 접근 | 방식 | 장점 | 위험 | 결정 |
|---|---|---|---|---|
| 고정 오류 사건형 정비 | 검수된 기준봉에서 한 오류를 찾고 버튼으로 고친 뒤 같은 물체를 전후 비교 | 저학년에게 원인과 결과가 분명하고 드래그 없이 접근 가능하며 모든 오류를 결정적으로 검증 가능 | 자유 측정 느낌이 약해 실제 활동 연결이 필요 | **MVP 채택** |
| 자유 자 만들기 샌드박스 | 학생이 눈금·단위칸을 직접 그리거나 드래그해 자를 제작 | 창작과 탐색 자유도가 큼 | 정밀 드래그, 무한 오류 조합, 모바일·키보드 접근성, 우연한 맞춤 문제가 큼 | MVP 제외 |
| 카메라 AR 길이 측정 | 카메라로 실제 물건 길이를 추정 | 실생활 연결과 몰입감이 큼 | 보정·권한·개인정보·정확도·기기 차이 문제가 큼 | 제외 |
| 숫자 읽기 퀴즈 | 자 그림 끝 숫자를 고름 | 구현이 매우 빠름 | 시작점·단위 반복·틈·겹침의 이유를 배우지 못하고 끝 숫자 맞히기로 축소됨 | 사용하지 않음 |

### 채택안의 핵심 이유

- 같은 물체를 고장난 도구와 고친 도구로 재므로 물체가 아니라 측정 절차가 결과를 바꾼다는 점이 보입니다.
- 시작점, 틈, 겹침, 단위 크기, 눈금 사이 세기를 각각 독립 미션으로 통제할 수 있습니다.
- 수리 버튼은 검수된 조작만 적용하므로 정밀 드래그나 우연한 픽셀 정렬이 필요하지 않습니다.
- 모든 기기에서 같은 정수 모형 좌표를 사용하여 화면 확대·축소가 답을 바꾸지 않습니다.
- 서버, 카메라, 실제 화면 보정 없이 안정적인 정적 MVP로 구현할 수 있습니다.

## 5. 기존 앱과의 차별화

| 기존 앱·계획 | 겹칠 수 있는 요소 | 측정 기준봉 정비소의 차별화 기준 |
|---|---|---|
| 째깍째깍 시간 탐험대 | 눈금, 바늘, 단위 읽기, 저학년 조작 | 시계의 시·분과 바늘 회전을 다루지 않습니다. 길이 단위의 시작점·간격·겹침·동일 반복을 검사합니다. |
| 도전! 디지털 수평잡기 실험실 | 거리, 막대, 물체 비교 | 받침점 거리·무게·시소 균형을 계산하지 않습니다. 길이를 재는 기준봉 자체의 오류와 절차를 고칩니다. |
| 십 묶음 택배 터미널 | 같은 단위 묶음, 저학년 버튼 조작 | 십 묶음·낱개·자릿값·묶기·풀기를 다루지 않습니다. 동일 길이 단위가 빈틈없이 반복되는 구조를 다룹니다. |
| 분류 기준 뒤집개 | 같은 자료를 기준에 따라 비교 | 단추 분류·개수 세기·기준 변경을 하지 않습니다. 같은 물체를 같은 길이 단위로 재는 일관성을 다룹니다. |
| 요리조리 도형 변신 마법사 | 길이와 도형 조작 | 꼭짓점 드래그·각도·도형 성질을 다루지 않습니다. 물체 모양은 고정하고 측정 도구만 정비합니다. |
| AI 각도 챌린지 | 측정 도구와 눈금 | 각도기·각도 어림·삼각형 내각을 다루지 않습니다. 선분 길이의 단위 구간과 시작점을 다룹니다. |
| 생활도구 리디자인 클리닉 | 고장난 도구 개선, 전후 비교 | 사용자 불편·재료·모양·기능을 창의적으로 바꾸지 않습니다. 측정 단위의 수학적 일관성만 복구합니다. |

### 차별화 검수 규칙

- 핵심 순환은 `물체 길이 어림 → 고장난 기준봉 살피기 → 오류 한 가지 찾기 → 버튼으로 수리 → 눈금 사이 세기 → 다시 측정 → 전후 값의 이유 설명`입니다.
- 시계 바늘, 받침점, 무게, 십 묶음, 단추 분류, 각도기, 꼭짓점 드래그를 사용하지 않습니다.
- 장식적 수리 게임이 아니라 시작점·단위 반복·눈금 구간의 수학적 이유를 항상 보여 줍니다.
- 최종 산출물은 시간 기록이나 도구 처방전이 아니라 읽기 전용 `측정 정비 기록`입니다.

## 6. MVP 제품 계약

### 기본 정보

| 항목 | 결정 |
|---|---|
| 앱 이름 | 측정 기준봉 정비소 |
| 학생용 부제 | 같은 단위칸으로 바르게 재어요 |
| 주 대상 | 초등 1~2학년, 개인 또는 2인 모둠 |
| 1회 활동 시간 | 12~18분 |
| 콘텐츠 | 안내 활동 1개 + 고정 미션 5개 + 선택형 1m 관계 카드 |
| 대상 길이 | 2~12개의 정수 모형 cm 단위, 분수·소수 길이 없음 |
| 핵심 오류 | 시작점 어긋남, 틈, 겹침, 서로 다른 단위 크기, 눈금선 개수 혼동 |
| 핵심 조작 | 오류 선택, 0 맞추기, 틈 닫기, 겹침 풀기, 단위 크기 통일, 눈금 사이 선택 |
| 드래그 | 필수 조작으로 사용하지 않음 |
| 입력 | 버튼·라디오 그룹·선택 카드만 사용, 자유 글 없음 |
| 저장 | React 메모리만 사용하며 새로고침 시 초기화 |
| 네트워크 | 초기 정적 자원 외 요청 없음 |
| 개인정보 | 이름·학번·학급·사진·음성·위치·자유 입력 수집 없음 |
| 평가 | 점수 없이 오류 발견·수리·재측정·이유 확인 여부만 요약 |
| 최종 산출물 | 어림, 고장 종류, 잘못 표시된 값, 수리 동작, 바른 측정값, 전후 차이 이유를 담은 `측정 정비 기록` |

### 학생 학습 목표

학생은 활동 후 다음을 할 수 있어야 합니다.

1. 두 물체의 한쪽 끝을 맞추어 어느 것이 더 길거나 짧은지 비교합니다.
2. 화면의 1cm 모형칸과 실제 1cm를 구분합니다.
3. 물체 한 끝을 기준봉의 0과 맞춥니다.
4. 같은 크기의 단위칸이 틈과 겹침 없이 반복되어야 함을 설명합니다.
5. 단위칸 사이의 틈과 겹침을 찾아 고칩니다.
6. 서로 다른 크기의 단위칸으로 잰 값은 믿고 비교하기 어렵다는 점을 설명합니다.
7. 눈금선이 아니라 눈금 사이의 단위 구간을 세어 길이를 읽습니다.
8. 같은 물체를 바른 도구로 다시 재어 정비 전후 값이 달라진 이유를 말합니다.
9. 측정 전에 길이를 어림하고 바르게 잰 값과 비교합니다.
10. 1m와 100cm의 관계를 모형 묶음 카드로 확인합니다.

### MVP에서 제외

- 실제 화면 길이 보정, 기기별 DPI 계산, 화면을 실물 자처럼 사용하는 기능
- 카메라·AR·사진 업로드를 이용한 실제 물체 자동 측정
- 분수·소수 길이, mm, km, 둘레·넓이, 길이의 덧셈·뺄셈
- 0이 부서진 자에서 두 눈금의 차로 재는 고급 측정
- 비스듬한 물체, 곡선 길이, 둘레, 여러 선분 합성 측정
- 자유 눈금 그리기, 자유 크기 조절, 정밀 드래그, 핀치 확대 조작
- 100개 cm 눈금을 한 화면에서 직접 세게 하는 활동
- 실제 자·줄자·교실 물건을 앱 완료로 대신하는 평가
- 시간 제한, 점수, 생명, 별, 배지, 순위, 콤보
- 학생 계정, 학급 코드, 교사 대시보드, 클라우드 저장, 분석 로그
- 생성형 AI 설명, 자유 서술 자동 채점, 외부 자료 검색
- GitHub 저장소 생성, 배포, HVC 등록, 정적 갤러리 동기화

## 7. 결정적 측정 모형

### 공통 좌표계

- 모든 미션은 `VIEWBOX_WIDTH = 1200`, `VIEWBOX_HEIGHT = 500`의 고정 SVG 좌표계를 사용합니다.
- 내부 기본 단위 `MODEL_UNIT = 120`을 1개의 모형 cm 구간으로 사용합니다.
- 틈·겹침·서로 다른 크기를 표현할 때에도 모든 좌표는 정수 하위 단위로 저장합니다.
- 반응형 화면은 SVG 표시 크기만 바꾸며 내부 좌표와 측정 결과를 바꾸지 않습니다.
- CSS px, `getBoundingClientRect()`, 기기 DPI를 측정 계산에 사용하지 않습니다.
- 학생 화면의 `cm` 표시는 물리적 화면 크기가 아니라 문제 데이터의 모형 단위 이름입니다.

### 기준봉 데이터 구조

```text
MeasurementMission
├─ id, order, title, contentVersion
├─ object
│  └─ startUnit, lengthUnits, label, estimateOptions[]
├─ brokenTool
│  ├─ originUnit
│  ├─ intervals[]
│  │  └─ start, end, labelStart, labelEnd
│  └─ declaredReading
├─ expectedFaults[]
├─ allowedRepairs[]
├─ repairedTool
├─ correctReading
├─ beforeAfterExplanation
└─ teacherNote, reviewRecord
```

### 바른 기준봉 불변 조건

- 첫 경계의 숫자는 0이며 물체 시작 경계와 정렬됩니다.
- 모든 단위 구간의 길이는 정확히 `MODEL_UNIT`입니다.
- 앞 구간의 끝과 다음 구간의 시작이 같아 틈과 겹침이 없습니다.
- 경계 라벨은 0부터 1씩 증가합니다.
- 물체의 다른 끝은 정수 단위 경계와 일치합니다.
- 측정값은 물체가 차지한 단위 구간 수이며 눈금선 개수와 구분됩니다.
- 고친 도구의 측정값은 문제 데이터의 `object.lengthUnits`와 일치합니다.

### 오류 판정

| 내부 코드 | 판정 조건 | 학생용 설명 | 일반화 주의 |
|---|---|---|---|
| `origin-misaligned` | 물체 시작과 도구의 0 경계가 다름 | `물체 한 끝과 0이 맞지 않아요.` | 0이 아닌 눈금에서 빼기로 재는 방법은 이 앱 범위 밖 |
| `gap` | 다음 구간 시작이 앞 구간 끝보다 큼 | `단위칸 사이에 빈틈이 있어요.` | 특정 미션의 잘못된 값 방향만 설명 |
| `overlap` | 다음 구간 시작이 앞 구간 끝보다 작음 | `단위칸이 겹쳐 같은 곳을 두 번 덮어요.` | 겹침 크기에 따라 값 오류 정도가 달라질 수 있음 |
| `unequal-unit` | 구간 길이가 기준 단위와 다름 | `단위칸 크기가 서로 달라요.` | 더 크다·작다 방향보다 값 신뢰 불가를 우선 |
| `boundary-count` | 눈금선 수를 길이로 선택 | `선이 아니라 선 사이의 칸을 세어요.` | 0과 끝 숫자를 단순 더하지 않음 |
| `label-sequence` | 경계 라벨이 빠지거나 중복·역순 | `눈금 숫자 차례가 바르지 않아요.` | 숫자만 고쳐도 실제 간격 오류가 해결되는 것은 아님 |

### 수리 동작

- `align-origin`: 물체 시작 또는 기준봉 0을 검수된 위치로 맞춥니다.
- `close-gap`: 지정된 빈 공간을 없애고 뒤 구간을 연속되게 옮깁니다.
- `remove-overlap`: 겹친 구간을 풀어 앞 구간 끝에서 이어지게 합니다.
- `normalize-units`: 검수된 기준 단위 크기로 모든 구간을 맞춥니다.
- `restore-labels`: 실제 경계 순서에 맞게 0부터 숫자를 복원합니다.
- `count-intervals`: 경계선 대신 물체가 차지한 구간을 하나씩 표시합니다.
- 모든 수리는 미션 데이터의 허용 목록에서만 적용하며 자유 좌표 입력을 받지 않습니다.

### 잘못된 값의 방향 설명

- 시작점 사건에서는 끝 숫자만 읽으면 실제 길이보다 큰 숫자를 읽는 검수 사례를 사용합니다.
- 틈 사건에서는 빈 공간까지 포함한 반복 단위를 세어 실제보다 적은 단위칸으로 물체를 덮는 검수 사례를 사용합니다.
- 겹침 사건에서는 같은 물체 부분을 여러 단위칸이 덮어 실제보다 많은 단위칸을 세는 검수 사례를 사용합니다.
- 서로 다른 단위 크기 사건에서는 값 방향을 하나로 단정하지 않고 `측정값을 믿고 비교하기 어려움`을 핵심 결과로 사용합니다.
- 모든 방향 설명은 해당 미션 배치에만 적용하며 `틈이면 언제나 정확히 1 작다` 같은 공식을 만들지 않습니다.

### 1m 관계 모형

- 1m 모형 막대는 10cm 묶음 10개로 시각화합니다.
- 각 10cm 묶음은 다시 1cm 모형칸 10개라는 텍스트 관계를 제공합니다.
- 100개 낱눈금을 모바일 화면에 동시에 세게 하지 않습니다.
- `1m = 100cm` 관계만 확인하고 `몇 m 몇 cm ↔ 몇 cm` 변환 퀴즈는 MVP 핵심에서 제외합니다.
- 화면의 1m 막대가 실제 물리적 1m가 아님을 카드 상단에 표시합니다.

## 8. 핵심 학습 순환과 피드백

### 미션별 8단계

1. **물체 살펴보기:** 한쪽 끝을 맞춘 두 물체를 비교하거나 목표 물체를 확인합니다.
2. **길이 어림하기:** 가까운 세 선택지 또는 기준 모형과 비교해 예상합니다.
3. **고장난 기준봉 보기:** 0, 단위칸, 틈·겹침, 눈금 숫자를 순서대로 살핍니다.
4. **오류 찾기:** 미션의 오류 후보 중 가장 먼저 고칠 한 가지를 선택합니다.
5. **버튼으로 수리하기:** 검수된 수리 동작을 적용합니다.
6. **눈금 사이 세기:** 물체가 차지한 같은 크기 구간을 하나씩 확인합니다.
7. **다시 측정하기:** 고장난 표시값과 바른 측정값을 비교합니다.
8. **이유 기록하기:** 시작점·틈·겹침·단위 크기·눈금선 중 결과가 달라진 이유를 고릅니다.

### 피드백 코드

| 코드 | 조건 | 학생 피드백 방향 | 다음 이동 |
|---|---|---|---|
| `repaired-and-explained` | 오류를 고치고 바른 구간 수와 이유를 연결함 | `틈을 닫으니 같은 크기 칸 5개가 빈틈없이 이어졌어요.` | 기록 후 다음 |
| `fault-not-found` | 현재 미션의 오류와 다른 곳을 선택 | 0→칸 크기→틈·겹침 순으로 한 단서만 안내 | 다시 살피기 |
| `partial-repair` | 복합 미션에서 일부 오류만 고침 | 남아 있는 오류 종류 수만 안내하고 정답 위치는 바로 공개하지 않음 | 계속 수리 |
| `boundary-counted` | 0부터 끝까지 눈금선 수를 선택 | 경계선과 사이 구간을 번갈아 강조 | 다시 세기 |
| `unit-screen-confusion` | 화면 칸을 실제 1cm로 표현 | `화면에서는 1cm를 나타내는 모형칸이에요.` | 모형 문구 확인 |
| `unsupported-reading` | 서로 다른 단위 크기에서 숫자를 확정 | `단위 크기가 같지 않아 이 값은 믿고 비교하기 어려워요.` | 단위 통일 |
| `content-error` | 좌표·구간·라벨·수리 데이터가 잘못됨 | 학생 답으로 처리하지 않고 학습 자료 오류 안내 | 다시 시작 또는 안전한 다음 미션 |

### 평가 원칙

- 첫 어림이 실제 측정값과 달라도 오답 횟수로 저장하지 않습니다.
- 빠르게 숫자를 맞히는 것보다 오류를 찾고 단위 구간으로 이유를 설명하는 것을 우선합니다.
- 복합 미션에서 수리 순서가 달라도 모든 오류를 고치면 인정합니다.
- `틀린 자`보다 `고장난 기준봉`, `오답`보다 `다시 살펴볼 곳`을 사용합니다.
- 같은 미션을 다시 해도 실패 횟수·소요 시간·힌트 사용 횟수를 결과에 표시하지 않습니다.

## 9. 미션 콘텐츠 계획

### 안내 활동: 같은 칸을 이어 재어요

| 항목 | 계획 |
|---|---|
| 내부 ID | `tutorial-equal-unit-tiles` |
| 물체 | 길이 4 모형 cm인 종이띠 |
| 도구 | 같은 크기 단위칸 4개가 0부터 틈·겹침 없이 이어진 기준봉 |
| 활동 | 물체 한 끝과 0 맞추기, 단위 구간 4개 세기 |
| 핵심 문장 | `선은 5개지만 선 사이의 같은 칸은 4개예요.` |
| 모형 안내 | 화면의 칸은 실제 1cm 크기가 아니라 1cm를 나타내는 모형 |

### 미션 1: 출발점이 어긋난 기준봉

| 항목 | 계획 |
|---|---|
| 내부 ID | `misaligned-zero-start` |
| 물체 | 길이 5 모형 cm |
| 고장 상태 | 물체 시작이 눈금 1에 있고 다른 끝이 눈금 6에 놓임 |
| 잘못 읽기 | 끝 숫자 6만 보고 6cm라고 함 |
| 수리 | 물체 한 끝과 0 맞추기 |
| 바른 결과 | 같은 단위 구간 5개, 5 모형 cm |
| 핵심 이유 | 시작 숫자가 0이 아니면 저학년 방식에서 끝 숫자만 길이로 읽을 수 없음 |
| 범위 문구 | 다른 눈금에서 시작해 차를 구하는 방법은 나중에 배울 수 있으나 이 앱에서는 0에 맞춤 |

### 미션 2: 틈이 벌어진 단위칸

| 항목 | 계획 |
|---|---|
| 내부 ID | `gapped-unit-tiles` |
| 물체 | 길이 5 모형 cm |
| 고장 상태 | 같은 크기 단위칸 사이에 검수된 빈 공간이 있어 4칸 표시가 물체 끝까지 닿음 |
| 잘못 표시 | 4 모형 cm처럼 표시 |
| 수리 | `틈 닫기` 버튼으로 단위칸을 연속 배치하고 필요한 한 칸을 보충 |
| 바른 결과 | 단위칸 5개, 5 모형 cm |
| 핵심 이유 | 빈 공간은 단위칸이 아니며 길이에 포함해 세면 안 됨 |

### 미션 3: 겹쳐진 단위칸

| 항목 | 계획 |
|---|---|
| 내부 ID | `overlapping-unit-tiles` |
| 물체 | 길이 5 모형 cm |
| 고장 상태 | 6개 단위칸 일부가 서로 겹쳐 같은 물체 구간을 중복해서 덮음 |
| 잘못 표시 | 6 모형 cm처럼 표시 |
| 수리 | `겹침 풀기`로 각 칸을 앞 칸 끝에서 시작하게 정렬 |
| 바른 결과 | 물체를 덮는 같은 단위칸 5개, 5 모형 cm |
| 핵심 이유 | 겹친 부분을 두 번 세면 단위 개수가 실제보다 많아질 수 있음 |

### 미션 4: 크기가 들쭉날쭉한 단위칸

| 항목 | 계획 |
|---|---|
| 내부 ID | `unequal-unit-widths` |
| 물체 | 길이 6 모형 cm |
| 고장 상태 | `작은 칸·큰 칸·작은 칸`이 섞여 있으나 모두 1칸이라고 표시 |
| 첫 판단 | 숫자를 바로 확정하지 않고 `이 도구의 값은 믿기 어려움` 선택 |
| 수리 | 모든 단위칸을 같은 `MODEL_UNIT` 크기로 맞춤 |
| 바른 결과 | 같은 단위칸 6개, 6 모형 cm |
| 핵심 이유 | 표준 단위는 이름뿐 아니라 크기가 같아야 다른 측정과 비교 가능 |

### 미션 5: 눈금선 수리 종합 점검

| 항목 | 계획 |
|---|---|
| 내부 ID | `boundary-and-combined-repair` |
| 물체 | 길이 7 모형 cm |
| 어림 | 5·7·9 모형 cm 중 가까운 값 선택 |
| 고장 상태 | 0 시작은 맞지만 눈금 라벨 하나가 중복되고 단위칸 한 곳에 작은 겹침이 있음 |
| 1차 수리 | 겹침을 풀어 같은 단위 구간 복원 |
| 2차 수리 | 경계 라벨을 0부터 7까지 복원 |
| 바른 결과 | 눈금선 8개, 눈금 사이 단위 구간 7개, 길이 7 모형 cm |
| 핵심 이유 | 선 개수와 구간 수를 구분하고, 숫자와 실제 간격을 모두 확인해야 함 |
| 완주 문장 | `물체는 그대로였고 기준봉의 간격과 숫자를 고쳐 값이 바르게 나타났어요.` |

### 선택형 관계 카드: 1m와 100cm

| 항목 | 계획 |
|---|---|
| 내부 ID | `meter-centimeter-relation-card` |
| 표현 | 1m 모형 막대 = 10cm 묶음 10개 = 1cm 모형칸 100개 |
| 조작 | 10cm 묶음 접기·펼치기 버튼, 직접 100번 세기 없음 |
| 질문 | `1m는 몇 cm와 같을까요?` 단일 확인 |
| 범위 | 복합 단위 변환·길이 덧셈·뺄셈 없음 |
| 모형 안내 | 화면 막대의 실제 보이는 길이는 1m가 아님 |

### 미션 분포와 콘텐츠 수용 기준

- 안내 1개, 시작점 1개, 틈 1개, 겹침 1개, 다른 단위 크기 1개, 복합·눈금선 1개를 제공합니다.
- 일반 미션의 물체 길이는 5·6·7 모형 cm로 제한하여 계산보다 절차에 집중합니다.
- 초기 미션 1~4는 핵심 오류가 정확히 하나이며 마지막 미션만 두 오류를 순서와 무관하게 수리합니다.
- 모든 수리 후 물체 길이와 단위 구간 수가 정확히 일치합니다.
- 오류 카드 위치와 정답 숫자를 분산하여 위치 맞히기 패턴을 막습니다.

## 10. 상태 모델과 도메인 계약

### 세션 단계

```text
start
→ model-guide
→ tutorial
→ mission-object
→ estimate
→ inspect-tool
→ choose-fault
→ apply-repair
→ count-intervals
→ measure-again
→ explain-difference
→ next-mission | meter-card | final-record
```

### 핵심 타입 계획

```ts
type FaultCode =
  | 'origin-misaligned'
  | 'gap'
  | 'overlap'
  | 'unequal-unit'
  | 'boundary-count'
  | 'label-sequence'

type RepairAction =
  | 'align-origin'
  | 'close-gap'
  | 'remove-overlap'
  | 'normalize-units'
  | 'restore-labels'
  | 'count-intervals'

type UnitInterval = {
  id: string
  start: number
  end: number
  startLabel: number
  endLabel: number
}
```

### 세션 상태

- 현재 미션과 단계
- 모형과 실물 차이 안내 확인 여부
- 학생의 어림 선택
- 학생이 선택한 오류 코드
- 적용한 수리 동작 순서
- 현재 기준봉 구간과 라벨 상태
- 선택한 단위 구간 수와 측정값
- 정비 전후 차이 이유 카드
- 완료한 미션별 읽기 전용 기록
- 1m 관계 카드 확인 여부
- 교사용 안내와 업데이트 내역 대화상자 상태

### 상태 불변 조건

- 모형 안내 확인 전에는 첫 미션을 시작할 수 없습니다.
- 어림 선택 전에는 고장난 표시값을 정답처럼 공개하지 않습니다.
- 현재 미션이 허용하지 않은 수리 동작은 실행할 수 없습니다.
- 남은 오류가 있으면 바른 측정 단계로 이동할 수 없습니다.
- 수리 뒤에는 현재 구간 배열을 다시 검사하여 바른 기준봉 불변 조건을 확인합니다.
- 눈금 사이 세기 전에는 최종 측정값을 제출할 수 없습니다.
- 이전 단계로 돌아가 수리를 바꾸면 이후 구간 선택·측정·설명을 무효화합니다.
- 화면 확대와 너비 변경은 모형 좌표와 측정값을 바꾸지 않습니다.
- 결과 기록에는 학생 이름, 자유 입력, 오답 횟수, 소요 시간을 저장하지 않습니다.

## 11. 화면 구성과 학습 흐름

### 11.1 공통 화면 구조

| 영역 | 주요 내용 | 설계 원칙 |
|---|---|---|
| 상단 바 | 앱 이름, 현재 미션, 진행 단계, 도움말, 업데이트 내역 | 진행률을 경쟁 점수처럼 표현하지 않음 |
| 안내 영역 | 지금 해야 할 일 한 문장, 필요할 때만 펼치는 예시 | 한 화면에 한 가지 행동만 요구 |
| 작업대 | 물체와 측정 기준봉 모형, 시작점·경계선·단위 구간 표시 | 화면 속 모형임을 항상 확인할 수 있게 표시 |
| 정비 도구함 | 오류 찾기 및 수리 버튼 | 드래그 없이 버튼·키보드로 조작 가능 |
| 설명 영역 | 정비 전후 결과와 달라진 이유 선택 | 정답 숫자보다 측정 절차를 먼저 설명 |
| 하단 이동 | 이전, 확인, 다음 | 단계가 끝나기 전 다음 버튼은 비활성화하고 이유 제공 |

### 11.2 첫 실행 안내

1. `이 화면의 칸은 실제 자의 1cm가 아니라, 1cm를 나타내는 모형칸이에요.`라고 안내합니다.
2. 실제 물체를 화면에 대지 않도록 그림과 문장으로 알립니다.
3. 같은 크기의 단위가 빈틈이나 겹침 없이 반복되어야 한다는 튜토리얼을 제공합니다.
4. 경계선 수와 그 사이 단위 구간 수가 다름을 손가락 이동 애니메이션으로 보여 줍니다.
5. 안내 확인 후에만 첫 미션으로 이동합니다.

### 11.3 미션 한 회의 흐름

| 단계 | 학생 행동 | 시스템 반응 |
|---|---|---|
| 1. 물체 살펴보기 | 물체 길이를 `더 짧다·비슷하다·더 길다` 또는 가까운 정수로 어림 | 어림은 정오 판정하지 않고 이후 결과와 비교용으로만 보관 |
| 2. 고장 찾기 | 시작점, 틈, 겹침, 단위 크기, 눈금 읽기 중 의심 지점 선택 | 선택한 곳에 번호표를 붙이고 근거 문장 제공 |
| 3. 수리하기 | 미션에 허용된 수리 버튼을 순서대로 실행 | 바뀐 부분을 전후 겹침 그림으로 짧게 표시 |
| 4. 다시 재기 | 물체의 시작과 끝 사이 단위 구간을 버튼으로 하나씩 셈 | 선택된 구간을 색과 무늬로 함께 강조 |
| 5. 이유 설명 | 결과가 달라진 이유 카드를 선택 | 숫자와 절차 근거가 함께 맞을 때 완료 |
| 6. 정비 기록 | 정비 전후 그림과 핵심 원칙 확인 | 개인정보 없이 현재 세션에서만 읽기 전용 기록 제공 |

### 11.4 드래그 없는 조작

- `0에 맞추기`, `틈 닫기`, `겹친 칸 펴기`, `같은 크기로 맞추기`, `라벨 바로잡기` 버튼을 사용합니다.
- 여러 후보가 있을 때는 기준봉 위의 번호 버튼을 먼저 선택하고 수리 동작을 누릅니다.
- 단위 구간 세기는 각 구간 내부의 큰 버튼 또는 `이전 구간·다음 구간` 버튼으로 수행합니다.
- 잘못 누른 경우 `한 단계 되돌리기`를 제공하며, 전체 초기화는 확인 대화상자를 거칩니다.
- 포인터 이동 정밀도나 드래그 능력이 학습 성취를 좌우하지 않도록 합니다.

### 11.5 결과 설명 카드

결과 카드에는 다음 세 요소를 항상 함께 표시합니다.

1. `무엇이 잘못되었나요?` — 시작점, 틈, 겹침, 단위 크기, 라벨 또는 구간 세기
2. `어떻게 고쳤나요?` — 학생이 적용한 수리 동작 순서
3. `왜 결과가 달라졌나요?` — 같은 크기의 단위를 빈틈·겹침 없이 반복하고 시작과 끝 사이 구간을 세었기 때문이라는 설명

틈과 겹침 미션은 해당 고정 사례에서 나타난 방향만 설명합니다. `틈이 있으면 언제나 작게 나온다` 또는 `겹치면 언제나 크게 나온다`와 같은 일반화 문장은 사용하지 않습니다.

## 12. 시각 및 언어 디자인

### 12.1 시각 콘셉트

- 밝은 목재 작업대와 정비 도구 스티커를 사용하되 기계 수리 게임처럼 복잡하게 꾸미지 않습니다.
- 물체, 기준봉, 경계선, 단위 구간을 서로 다른 형태로 구분합니다.
- 고장 표시는 경고색뿐 아니라 `틈`, `겹침`, `크기 다름` 아이콘과 짧은 글자를 함께 사용합니다.
- 선택한 단위 구간은 색상, 굵은 윤곽선, 사선 무늬를 함께 사용합니다.
- 성공 효과는 1초 안팎의 간단한 정비 도장으로 제한합니다.

### 12.2 저학년 문장 원칙

- 한 문장은 가능한 한 20자 안팎으로 작성합니다.
- `단위의 일관성` 대신 `같은 크기의 칸`, `기점` 대신 `재기 시작한 곳`을 먼저 사용합니다.
- 버튼은 동사형으로 씁니다: `틈 닫기`, `다시 재기`, `이유 고르기`.
- 오답 메시지는 `틀렸어요`보다 `아직 틈이 한 곳 남았어요`처럼 다음 행동을 알려 줍니다.
- 숫자만 제시하지 않고 `경계선 6개, 그 사이 칸 5개`처럼 대상을 함께 말합니다.

### 12.3 글자와 조작 크기

- 기본 본문은 18px 이상, 핵심 수치는 24px 이상을 권장합니다.
- 모든 조작 대상은 최소 44×44 CSS 픽셀을 확보합니다.
- 숫자와 단위 사이를 명확히 띄우고, `1cm`와 `1m`의 글자 모양을 쉽게 구별합니다.
- 긴 안내는 기본 화면에서 접고, `도움말 보기`로 다시 열 수 있게 합니다.

## 13. 접근성 및 반응형 계획

### 13.1 키보드와 보조 기술

- 모든 기능은 `Tab`, `Shift+Tab`, `Enter`, `Space`, 방향키만으로 완료할 수 있어야 합니다.
- 기준봉의 각 구간과 오류 지점은 논리적인 DOM 순서를 갖는 실제 버튼으로 제공합니다.
- SVG는 시각 표현으로 사용하고, 같은 정보를 DOM 텍스트로 함께 제공합니다.
- 예: `물체 시작은 0, 끝은 5, 같은 간격은 5개입니다.`
- 단계가 바뀌면 제목으로 초점을 이동하고, 정비 결과는 `aria-live="polite"`로 알립니다.
- 오류 메시지는 해당 선택 영역과 프로그램적으로 연결합니다.

### 13.2 반응형 화면

- 320px 너비에서도 가로 스크롤 없이 핵심 흐름을 완료할 수 있어야 합니다.
- 작은 화면에서는 작업대 아래에 도구함을 배치하고, 기준봉은 `viewBox` 비율로 축소합니다.
- 축소 때문에 구간 버튼이 작아질 경우 기준봉 아래에 큰 번호 버튼 목록을 별도로 제공합니다.
- 200% 확대에서도 안내, 작업대, 버튼이 겹치거나 잘리지 않아야 합니다.
- 세로 화면에서 미션 완료에 필수인 정보가 동시에 너무 멀리 떨어지지 않도록 고정 요약줄을 둡니다.

### 13.3 감각 및 움직임 대응

- 색만으로 정상·고장을 구분하지 않습니다.
- `prefers-reduced-motion`에서는 이동 애니메이션을 즉시 전환으로 바꿉니다.
- 고대비 및 강제 색상 모드에서도 선택 테두리와 버튼 상태를 확인할 수 있게 합니다.
- 효과음은 MVP에서 제외하며, 자동 재생 미디어도 사용하지 않습니다.

## 14. 개인정보, 보안 및 오프라인 원칙

- 로그인, 이름, 학년, 반, 자유 입력을 수집하지 않습니다.
- 카메라, 마이크, 위치, 파일 업로드 권한을 요청하지 않습니다.
- 학습 결과, 오답 횟수, 이용 시간을 로컬 저장소나 쿠키에 저장하지 않습니다.
- 분석 도구, 광고, 외부 추적 스크립트를 사용하지 않습니다.
- 외부 글꼴과 CDN 없이 빌드 결과에 필요한 자산을 포함합니다.
- 사용자 입력을 서버로 전송하지 않는 정적 웹앱으로 구성합니다.
- 네트워크 연결이 끊겨도 이미 열린 학습 세션은 계속 사용할 수 있게 합니다.
- 향후 저장 기능이 필요하면 별도 기획과 개인정보 검토를 먼저 진행합니다.

## 15. 권장 프로젝트 구조

향후 구현 승인이 난 경우 별도 저장소 `measurement-standard-rod-repair-shop`에 다음 구조를 권장합니다.

```text
measurement-standard-rod-repair-shop/
├── docs/
│   ├── curriculum-alignment.md
│   ├── math-model.md
│   ├── privacy.md
│   └── teacher-guide.md
├── public/
│   └── icons/
├── src/
│   ├── app/
│   │   ├── App.tsx
│   │   └── appReducer.ts
│   ├── components/
│   │   ├── AppHeader.tsx
│   │   ├── Dialog.tsx
│   │   └── StepNavigation.tsx
│   ├── content/
│   │   ├── missions.ts
│   │   ├── explanations.ts
│   │   └── validateContent.ts
│   ├── domain/
│   │   ├── measurementModel.ts
│   │   ├── repairEngine.ts
│   │   └── types.ts
│   ├── features/
│   │   ├── guide/
│   │   ├── inspection/
│   │   ├── repair/
│   │   ├── recount/
│   │   ├── result/
│   │   └── teacher/
│   ├── hooks/
│   ├── styles/
│   ├── tests/
│   └── main.tsx
├── tests/
│   └── e2e/
└── package.json
```

### 분리 원칙

- 좌표와 측정 계산은 `domain`에서만 처리하고 React 컴포넌트가 값을 임의 계산하지 않게 합니다.
- 미션 내용은 `content` 데이터로 분리하여 계산 코드와 섞지 않습니다.
- 검사, 수리, 다시 재기, 결과 화면을 기능 단위로 분리합니다.
- 코드 파일 하나가 500줄을 넘기 전에 데이터, 도메인 규칙, 화면 구성 요소로 나눕니다.
- 앱 상태 변경은 reducer와 순수 함수로 관리하여 같은 입력에 같은 결과가 나오게 합니다.

## 16. 테스트 및 교육 검증 계획

### 16.1 도메인 단위 테스트

| 검증 대상 | 필수 사례 |
|---|---|
| 정상 기준봉 | 시작과 끝 일치, 같은 너비, 틈·겹침 없음, 연속 라벨 |
| 시작점 오류 | 물체가 0이 아닌 눈금에 맞춰진 고정 사례 탐지와 수리 |
| 틈 | 정수 모형 좌표에서 빈 구간의 위치와 크기 탐지 |
| 겹침 | 두 구간의 교차 위치와 크기 탐지 |
| 단위 크기 | 다른 너비가 하나 이상 섞인 경우 측정 불신 처리 |
| 경계선 세기 | 경계선 수가 단위 구간 수보다 하나 많음을 확인 |
| 라벨 | 중복, 누락, 역순 라벨 탐지 |
| 수리 동작 | 허용된 오류만 바꾸며 다른 정상 구간을 훼손하지 않음 |
| 반복 실행 | 같은 수리 동작을 다시 적용해도 상태가 불안정해지지 않음 |
| 화면 독립성 | 뷰포트 너비와 확대율이 측정 결과를 바꾸지 않음 |

### 16.2 콘텐츠 검증

- 모든 미션에 물체 길이, 초기 구간, 정확한 오류 코드, 허용 수리 동작, 최종 결과가 있어야 합니다.
- 앞 미션은 한 가지 오류, 마지막 미션은 두 가지 오류만 포함합니다.
- 틈 미션의 고장 표시값은 4, 수리 뒤 결과는 5인지 확인합니다.
- 겹침 미션의 고장 표시값은 6, 수리 뒤 결과는 5인지 확인합니다.
- 단위 크기 오류에는 보편적인 결과 방향을 단정하는 문장이 없어야 합니다.
- `화면의 1cm`, `화면에서 실제 길이`처럼 오해를 일으키는 문장을 금지합니다.
- 0이 아닌 눈금에서 시작한 모든 측정이 수학적으로 불가능하다고 단정하지 않습니다.
- 모든 설명 카드가 오류, 수리, 결과 변화 이유를 함께 포함하는지 검사합니다.

### 16.3 컴포넌트 및 통합 테스트

- 버튼만으로 각 미션을 시작부터 끝까지 완료할 수 있어야 합니다.
- 허용되지 않은 수리 동작은 비활성화되고 이유를 읽을 수 있어야 합니다.
- 이전 단계로 돌아가 수리를 바꾸면 뒤 단계 데이터가 초기화되어야 합니다.
- 시각적 SVG와 텍스트 대체 설명의 숫자가 항상 같아야 합니다.
- 대화상자 열기, 닫기, 초점 복귀가 올바르게 작동해야 합니다.
- 업데이트 내역과 교사용 안내가 학습 상태를 잃지 않고 열려야 합니다.

### 16.4 브라우저 E2E 점검

1. 첫 실행에서 모형과 실제 자의 차이 안내를 건너뛸 수 없는지 확인합니다.
2. 튜토리얼에서 경계선과 단위 구간을 구별하는지 확인합니다.
3. 시작점, 틈, 겹침, 단위 크기, 복합 오류 미션을 각각 완료합니다.
4. 틈과 겹침 결과 설명이 해당 고정 사례에만 맞게 표현되는지 확인합니다.
5. 0이 아닌 시작점에 대한 설명이 과도하게 단순화되지 않았는지 확인합니다.
6. 선택 구간 수와 화면·텍스트 결과가 일치하는지 확인합니다.
7. `1m = 100cm` 관계 카드가 10묶음 구조로 이해되는지 확인합니다.
8. 키보드 전용, 320px 화면, 200% 확대, 움직임 감소 모드로 전체 흐름을 완료합니다.
9. 자동 접근성 검사와 실제 초점 이동 검사를 함께 수행합니다.
10. 학습 과정에서 저장소, 쿠키, 불필요한 외부 요청이 생기지 않는지 확인합니다.

### 16.5 향후 실행할 품질 명령

다음 명령은 구현 승인 뒤 프로젝트가 생성되었을 때만 실행합니다.

```bash
npm run lint
npm run typecheck
npm run validate:content
npm run test
npm run build
npm run test:e2e
```

현재 단계에서는 프로젝트나 코드가 없으므로 위 명령을 실행하지 않습니다.

## 17. 교사용 수업 연결

### 17.1 권장 수업 흐름

| 시점 | 활동 | 교사 질문 |
|---|---|---|
| 앱 사용 전 | 연필, 지우개 등 두 물체의 길이를 직접 비교 | `어느 것이 더 길다고 생각하나요?` |
| 도입 | 종이띠나 블록으로 같은 단위를 반복해 보기 | `칸 사이가 벌어지면 믿을 수 있을까요?` |
| 앱 활동 | 기준봉 오류를 찾아 고치고 다시 재기 | `숫자가 아니라 재는 방법에서 무엇이 바뀌었나요?` |
| 앱 사용 후 | 실제 자나 같은 길이의 종이 단위로 교실 물체 재기 | `화면 속 원칙이 실제 자에서도 어떻게 보이나요?` |
| 정리 | 잘 재기 위한 약속을 세 문장으로 말하기 | `시작, 단위, 끝 중 무엇을 확인해야 하나요?` |

### 17.2 교사용 핵심 안내

- 이 앱은 실제 자 사용을 대체하지 않고 측정 절차의 오류를 눈에 보이게 연습하는 도구입니다.
- 학생이 화면에 실물을 대어 재지 않도록 활동 전후에 분명히 안내합니다.
- `0에 맞추기`는 초보 측정을 안정적으로 익히기 위한 기본 절차입니다. 이후 학습에서는 두 눈금의 차를 이용할 수 있음을 교사용 안내에 덧붙입니다.
- 단위가 일정하지 않은 기준봉에서는 단순히 칸 개수를 세어 결과를 확정하지 않도록 질문합니다.
- 정답 속도나 점수보다 `어떤 절차가 잘못되었는지 말할 수 있는가`를 관찰합니다.

## 18. 업데이트 내역과 문서화

### 18.1 앱 내부 업데이트 내역

- 화면 상단 또는 하단의 눈에 잘 띄지만 학습을 방해하지 않는 위치에 `업데이트 내역` 버튼을 둡니다.
- 대화상자에 날짜, 버전, 학생이 이해할 수 있는 개선 내용을 최신순으로 표시합니다.
- 최초 구현 시에도 개발 날짜와 핵심 기능을 기록합니다.
- 수정할 때마다 접근성, 문장, 미션, 오류 수정 등 실제 변경 사항을 짧게 추가합니다.

예시 형식은 다음과 같습니다.

```text
2026-07-17 · v1.0.0
- 측정 기준봉 정비 미션 5개를 처음 만들었어요.
- 시작점, 틈, 겹침, 같은 단위, 눈금 사이 세기를 연습할 수 있어요.
```

### 18.2 저장소 문서

- `README.md`: 학습 목표, 실행법, 정적 배포법, 개인정보 원칙
- `docs/curriculum-alignment.md`: 성취기준과 미션별 연결 근거
- `docs/math-model.md`: 모형 좌표, 오류 판정, 경계선과 구간 규칙
- `docs/teacher-guide.md`: 실물 측정 전후 활동과 오개념 주의사항
- `docs/privacy.md`: 수집·저장·전송하지 않는 정보 명시
- 검증 기록: 콘텐츠 검수자, 검수 날짜, 확인한 사례와 남은 위험

## 19. 향후 구현 순서

다음은 구현 요청이 별도로 승인된 뒤 수행할 작업이며, 현재 요청에서는 실행하지 않습니다.

1. 별도 저장소를 만들고 Vite, React, TypeScript 기반 정적 앱 골격을 구성합니다.
2. 모형 좌표, 단위 구간, 오류 코드, 수리 동작 타입을 정의합니다.
3. 정상 기준봉 불변 조건과 오류 탐지 순수 함수를 작성합니다.
4. 허용된 수리만 적용하는 수리 엔진과 되돌리기 규칙을 작성합니다.
5. 미션 스키마와 빌드 전 콘텐츠 검증기를 작성합니다.
6. 튜토리얼, 고정 미션 5개, 선택형 1m 관계 카드 데이터를 입력하고 수학 검수합니다.
7. 단계 전환과 무효화 규칙을 포함한 앱 reducer를 작성합니다.
8. 안내, 점검, 수리, 다시 재기, 이유 설명 화면을 드래그 없이 구현합니다.
9. 결과 기록, 교사용 안내, 업데이트 내역 대화상자를 구현합니다.
10. 반응형, 키보드, 보조 기술, 움직임 감소 대응과 테스트를 추가합니다.
11. 저학년 문장 검수와 실제 자를 이용한 소규모 수업 사용성 점검을 진행합니다.
12. 모든 검증을 통과한 뒤에만 배포를 준비하며, HVC 등록은 별도 요청으로 처리합니다.

## 20. MVP 수용 기준

### 학습 기능

- 튜토리얼과 고정 미션 5개를 처음부터 끝까지 완료할 수 있습니다.
- 시작점, 틈, 겹침, 다른 단위 크기, 경계선과 구간 혼동을 각각 다룹니다.
- 마지막 미션은 두 오류를 순서대로 찾아 고치되 한 화면에서 세 오류 이상을 요구하지 않습니다.
- 학생이 정비 전후 결과가 달라진 이유를 절차 근거로 선택할 수 있습니다.
- `1m = 100cm` 관계는 선택형 묶음 카드로 확인할 수 있습니다.

### 수학적 정확성

- 정답은 CSS 픽셀이나 실제 화면 크기가 아니라 정수 모형 좌표로 계산합니다.
- 정상 기준봉은 같은 크기의 단위가 틈과 겹침 없이 반복됩니다.
- 경계선이 아니라 물체의 시작과 끝 사이 단위 구간을 셉니다.
- 0이 아닌 눈금에서 시작하는 모든 방법이 틀렸다고 단정하지 않습니다.
- 단위 크기가 일정하지 않을 때 결과 방향을 보편적으로 단정하지 않습니다.
- 화면의 모형칸을 물리적인 1cm로 오해하게 하는 표현이 없습니다.

### 사용성 및 접근성

- 드래그 없이 모든 미션을 완료할 수 있습니다.
- 키보드와 터치 조작 모두 지원하며 조작 대상은 최소 44×44 CSS 픽셀입니다.
- 320px 너비와 200% 확대에서도 필수 기능을 사용할 수 있습니다.
- SVG의 모든 학습 정보에 동등한 텍스트 설명이 있습니다.
- 색만으로 상태를 전달하지 않고 움직임 감소 설정을 존중합니다.

### 개인정보 및 기술

- 로그인, 개인정보 수집, 사용자 데이터 저장, 서버 통신이 없습니다.
- 외부 CDN과 추적 도구 없이 정적 빌드로 동작합니다.
- 콘텐츠 검증, 단위 테스트, 통합 테스트, E2E, 접근성 검사를 통과합니다.
- 앱 안에 개발 날짜와 개선 기록을 확인할 수 있는 `업데이트 내역`이 있습니다.
- 각 코드 파일은 500줄 미만을 유지합니다.

## 21. 주요 위험과 완화 방안

| 위험 | 영향 | 완화 방안 |
|---|---|---|
| 화면 속 칸을 실제 1cm로 오해 | 실제 측정 개념 왜곡 | 첫 실행 필수 안내, 모든 작업대에 `1cm 모형칸` 배지, 실물을 화면에 대지 말라는 문장 제공 |
| 0이 아닌 시작은 모두 틀렸다고 이해 | 이후 두 눈금 차 학습과 충돌 | 초보 기본 절차로 범위를 밝히고 교사용 안내에 차를 이용한 방법을 명시 |
| 눈금선을 칸으로 셈 | 1만큼 큰 결과 반복 | 경계선과 구간을 다른 모양으로 표시하고 튜토리얼에서 직접 비교 |
| 틈·겹침 결과 방향을 일반화 | 사례 밖에서 오개념 형성 | 고정 사례의 전후 수치만 설명하고 보편 문장 금지 검사 추가 |
| 여러 오류가 한꺼번에 등장 | 저학년 인지 부담 증가 | 앞 미션 한 오류, 마지막 미션 두 오류로 제한하고 단계별 도구만 공개 |
| 시간·균형·십 묶음 앱과 유사해짐 | 기존 앱과 학습 경험 중복 | 측정 절차 오류 탐지와 단위 반복의 신뢰성에만 초점 유지 |
| 생활도구 리디자인 활동처럼 보임 | 창의 설계 앱으로 오인 | 자유 제작 대신 고정된 수학 오류 검사·수리·재측정 흐름 사용 |
| 모바일 축소로 구간 조작이 어려움 | 작은 화면에서 학습 불가 | 별도의 큰 번호 버튼과 세로형 도구함 제공 |
| 정비 성공만 강조 | 원리 설명 없이 시행착오 게임화 | 완료 전에 이유 카드와 전후 비교를 필수로 요구 |
| 계획 문서가 구현 승인으로 오인됨 | 요청 범위 초과 | 문서 상단과 구현 순서에 계획 전용 상태 및 승인 게이트 명시 |

## 22. 구현 전 승인 게이트

구현을 시작하기 전에 다음 항목을 사용자와 확정합니다.

- 앱 이름: `측정 기준봉 정비소`
- 별도 저장소 이름: `measurement-standard-rod-repair-shop`
- 범위: 튜토리얼, 고정 미션 5개, 선택형 1m 관계 카드
- 핵심 표현: 화면 속 칸은 `1cm를 나타내는 모형칸`
- 조작 방식: 드래그 없는 버튼 중심 조작
- 기술 범위: 서버·로그인·저장 없는 정적 웹앱
- 배포 방식과 공개 범위
- HVC 등록 여부와 등록 시점
- 성취기준 및 사례에 대한 교사 또는 수학 교육 검수 담당자

승인 전에는 저장소 생성, 앱 코드 작성, 패키지 설치, 테스트 실행, 커밋, 푸시, 배포, HVC 등록을 하지 않습니다.

## 23. 계획 완료의 정의

이 문서는 다음 조건을 충족하면 계획 단계가 완료된 것으로 봅니다.

- 학습 목표와 기존 앱 대비 차별점이 명시되어 있습니다.
- 초등 1~2학년 수준의 측정 개념과 주의할 오개념이 정리되어 있습니다.
- 고정 미션과 정수 기반 수학 모델이 서로 일치합니다.
- 화면 흐름, 드래그 없는 조작, 접근성, 반응형 기준이 정의되어 있습니다.
- 개인정보를 다루지 않는 정적 MVP 범위가 확정되어 있습니다.
- 테스트, 콘텐츠 검수, 교실 수업 연결, 업데이트 내역 계획이 포함되어 있습니다.
- 향후 구현 순서와 구현 전 승인 항목이 분리되어 있습니다.

현재 산출물은 구현 계획 문서이며 앱 구현물은 포함하지 않습니다.
